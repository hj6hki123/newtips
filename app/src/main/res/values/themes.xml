<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Newtips" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="backgroundColor">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="strokeColor">#4B1C1B1B</item>
        <!-- Status bar color. -->
        <item name="actionBarStyle">@style/CustomActionBar</item>
        <item name="actionMenuTextAppearance">@style/CustomActionBarMenu</item>
        <item name="actionBarSize">50sp</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/taipei_r</item>
        <item name="fontFamily">@font/taipei_r</item>
    </style>

    <!-- 新增NoActionBar版本 -->
    <style name="Theme.Newtips.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="strokeColor">#4B1C1B1B</item>
        <!-- Status bar color. -->

        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/taipei_r</item>
        <item name="fontFamily">@font/taipei_r</item>
    </style>





    <style name="CustomTabWidgetText"
        parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">8pt</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- ActionBar styles -->
    <style name="CustomActionBar" parent="@style/Widget.Material3.ActionBar.Solid">
        <item name="titleTextStyle">@style/CustomActionBarTitle</item>
    </style>

    <!-- ActionBar title text -->
    <style name="CustomActionBarTitle" parent="@style/TextAppearance.Material3.ActionBar.Title">
        <!--<item name="android:textColor">@color/white</item>-->
        <item name="android:textSize">16sp</item>
    </style>

    <!-- ActionBar menu text -->
    <style name="CustomActionBarMenu">
        <item name="android:textSize">16sp</item>
    </style>

<style name="RecycleviewBackground" parent="Theme.AppCompat">
    <item name="backgroundColor" >#FFFFFF</item>
</style>





</resources>